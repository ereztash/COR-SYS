# רשימת בדיקות ידניות לפני עלייה לסביבת ייצור

> **הוראות**: סמן ✅ / ❌ ליד כל סעיף. תעד הערות בשדה "הערות" אם יש בעיה.

---

## 1. דיוק (Accuracy)

### 1.1 DSM + תוצאות אבחון

| # | בדיקה | תוצאה | הערות |
|---|-------|--------|-------|
| A1 | מילוי שאלון עם כל הפתולוגיות נמוכות → `severityProfile = healthy`, קוד `DR-1 ND-1 UC-1` | ☐ | |
| A2 | מילוי שאלון עם כל הפתולוגיות גבוהות → `severityProfile = systemic-collapse`, קוד `DR-3 ND-3 UC-3` | ☐ | |
| A3 | תוצאות `/assess/[token]/results` מציגות: DSM codes, comorbidity graph, פרוטוקולי התערבות | ☐ | |
| A4 | עמוד `/clients/[id]/plan` מציג DSM summary תואם לשאלון שנמלא | ☐ | |

### 1.2 Portfolio Analytics (דשבורד)

| # | בדיקה | תוצאה | הערות |
|---|-------|--------|-------|
| A5 | דשבורד עם לקוחות שיש להם אבחונים → כרטיסי healthy/at-risk/critical/systemic-collapse מוצגים | ☐ | |
| A6 | `mostCommonPathology` מוצג נכון (DR/ND/UC) כשיש ציון ≥ 7 | ☐ | |
| A7 | כשאין ציון ≥ 7 → מוצג "—" או שדה מוסתר | ☐ | |

---

## 2. אבטחה (Security)

### 2.1 Auth

| # | בדיקה | תוצאה | הערות |
|---|-------|--------|-------|
| S1 | גישה ל-`/` ללא session → redirect ל-`/login` | ☐ | |
| S2 | משתמש מחובר → גישה ל-`/login` מפנה ל-`/` (לא נשאר בדף login) | ☐ | |
| S3 | `/auth/callback` ללא פרמטר `code` → redirect ל-`/login?error=auth_callback_failed` | ☐ | |
| S4 | token לא קיים ב-`/assess/[token]` → 404 או הודעת "לא נמצא" | ☐ | |

### 2.2 ולידציה

| # | בדיקה | תוצאה | הערות |
|---|-------|--------|-------|
| S5 | ניסיון לשלוח שאלון עם token ריק (ישירות ל-API) → `{ ok: false, error: 'חסר token' }` | ☐ | |
| S6 | ניסיון `savePlanFromQuestionnaire` עם clientId לא-UUID → `{ ok: false, error: 'מזהה לקוח לא חוקי' }` | ☐ | |

---

## 3. חוויית משתמש (UX)

### 3.1 זרימות קריטיות

| # | בדיקה | תוצאה | הערות |
|---|-------|--------|-------|
| U1 | **לוגין**: לחיצה "התחבר עם Google" → redirect Google → חזרה → דשבורד | ☐ | |
| U2 | **דשבורד**: KPIs מוצגים (לקוחות, ספרינטים, הכנסות) | ☐ | |
| U3 | **Self-serve**: שליחת לינק → מילוי שאלון (כל השלבים) → שליחה → `/assess/[token]/results` עם תוצאות | ☐ | |
| U4 | **תוכנית**: `/clients/[id]/plan` — אם יש תוכנית: DSM summary + כרטיס "היסטוריית אבחונים" | ☐ | |
| U5 | **Longitudinal**: מילוי שאלון מחדש → שמירה → היסטוריה מתעדכנת עם רשומה חדשה | ☐ | |

### 3.2 מקרי קצה UI

| # | בדיקה | תוצאה | הערות |
|---|-------|--------|-------|
| U6 | שדה חובה ריק בשאלון → מניעת שליחה / הודעת שגיאה | ☐ | |
| U7 | **דשבורד ריק** (אין לקוחות): אין crash, מוצג state ריק תקין | ☐ | |
| U8 | **דשבורד ללא אבחונים**: portfolio analytics לא מופיע / מוסתר | ☐ | |
| U9 | **עמוד תוכנית ללקוח ללא תוכנית**: מוצג CTA למלא שאלון | ☐ | |

### 3.3 מובייל (viewport ≤ 390px)

| # | בדיקה | תוצאה | הערות |
|---|-------|--------|-------|
| M1 | `/login` — מוצג תקין, כפתור Google נגיש | ☐ | |
| M2 | `/` (דשבורד) — Sidebar הופך למבורגר / drawer | ☐ | |
| M3 | `/assess/[token]` — טאבים בשאלון נגללים, אין overflow | ☐ | |
| M4 | `/assess/[token]/results` — כרטיסי DSM לא יוצאים מהמסך | ☐ | |

---

## סיכום

| קטגוריה | עברו | נכשלו |
|---------|------|-------|
| דיוק (A1–A7) | | |
| אבטחה (S1–S6) | | |
| UX (U1–U9) | | |
| מובייל (M1–M4) | | |
| **סה"כ** | | |

**תאריך בדיקה**: ___________  
**בודק**: ___________  
**גרסה / commit**: ___________
